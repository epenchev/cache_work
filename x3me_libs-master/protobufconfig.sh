
# We need to use older version mostly because there is JS script code
# which uses the system version which is not the latest one
PROTOBUF_VER=2.6.1
PROTOBUF_DIR=protobuf-${PROTOBUF_VER}
PROTOBUF_INCLUDE_DIR=${PROTOBUF_DIR}/src/
PROTOBUF_LIB_DIR=${PROTOBUF_DIR}/src/.libs/
